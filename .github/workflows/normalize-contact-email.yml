name: Normalize contact email

on:
  push:
    paths:
      - ".github/workflows/normalize-contact-email.yml"

permissions:
  contents: write

jobs:
  normalize:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Replace legacy public contact emails
        shell: python
        run: |
          from pathlib import Path

          replacements = {
              "wangwanrong984@gmail.com": "wangwanrong@siggear.com",
              "wwang109@163.com": "wangwanrong@siggear.com",
          }
          allowed_suffixes = {
              ".md", ".markdown", ".txt", ".html", ".htm", ".xml",
              ".yml", ".yaml", ".json", ".py", ".js", ".ts", ".css",
              ".toml", ".ini